<%= form_for(@belt_level) do |f| %>
  <% if @belt_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@belt_level.errors.count, "error") %> prohibited this belt_level from being saved:</h2>

      <ul>
      <% @belt_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
